{
  "expect_flag": true,
  "description": "The canonical example: Left changes the tax rate, Right adds a discount deduction, both inside the same function on non-overlapping lines. Git merges cleanly. Both changes touch 'calculate' and interact through the shared 'tax' variable.",
  "expected_interactions": [
    {
      "kind": "same_entity_changed",
      "entities": [
        "calculate",
        "calculate"
      ]
    }
  ]
}
